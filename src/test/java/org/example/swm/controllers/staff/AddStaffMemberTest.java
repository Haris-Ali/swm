package org.example.swm.controllers.staff;

import org.example.swm.models.StaffMember;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.io.ByteArrayInputStream;

import static org.junit.jupiter.api.Assertions.*;

class AddStaffMemberTest {
//    StaffServiceMock staffServiceMock;
//    AddStaffMember addStaffMemberMock;

    @BeforeEach
    void setUp() {
//        staffServiceMock = new StaffServiceMock();
//        addStaffMemberMock = new AddStaffMember();
//        addStaffMemberMock.staffService = staffServiceMock;
    }

    @Test
    void addStaffMemberTest() {
//        String simulatedInput = "John Doe\n1.0\nMathematics\nJane Smith\n";
//        System.setIn(new ByteArrayInputStream(simulatedInput.getBytes()));
//
//        addStaffMemberMock.setup();
//        assertEquals(1, staffServiceMock.getAddedStaffMembers().size());
//        StaffMember addedStaffMember = staffServiceMock.getAddedStaffMembers().get(0);
//        assertEquals("John Doe", addedStaffMember.getName());
//        assertEquals(1.0, addedStaffMember.getContractType());
//        assertEquals("Mathematics", addedStaffMember.getSubjectArea());
//        assertEquals("Jane Smith", addedStaffMember.getLineManager());
    }
}